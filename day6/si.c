#include<stdio.h>
int main(){

    /*float principal;
    scanf("%f",&principal);
    float rate;
    scanf("%f",&rate);
    float time;
    scanf("%f",&time);
    printf("the simple intrest is %.2f",(principal*rate*time)/100);
    return 0;
    */
  int a=11,b=a++;
  printf("a=%d,b=%d",a,b);

}